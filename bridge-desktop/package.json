{
  "name": "pounce-bridge-app",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "Pounce Bridge — a tiny double-click desktop app that runs the bridge and shows a pairing QR.",
  "scripts": {
    "sync-server": "mkdir -p server && cp ../apps/bridge/server.mjs server/server.mjs",
    "dev": "bun run sync-server && electrobun dev",
    "build": "bun run sync-server && electrobun build"
  },
  "dependencies": {
    "electrobun": "^1.0.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
